<div class="container">
  <div class="row g-0 justify-content-center">
    <div class="col-md-6 col-sm-10 shadow-lg rounded px-0 my-5 bg-white">
      <h5 class="text-white bg-info p-4 mb-0 rounded-top">プロフィール編集フォーム</h5>
      <% if @user.errors.any? %>
        <h3><%= @user.errors.count %>errors prohibited this obj from being saved:</h3>
        <ul>
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>
      <div class="form-body p-4">
        <%= form_with model: @user do |f| %>
          <div class="form-group">
            <h6 class="text-dark">名前</h6>
            <%= f.text_field :name, class: "form-control border-info"%>
          </div>
          <div class="form-group">
            <h6 class="text-dark">プロフィール画像（任意）</h6>
            <%= f.file_field :profile_image, class: "form-control border-info", accept: "image/*" %>
          </div>
          <div class="form-group">
            <h6 class="text-dark">ひとこと</h6>
            <%= f.text_field :status_message, class: "form-control border-info"%>
          </div>
          <div class="form-group">
            <%= f.submit "変更を保存", class: "btn btn-lg btn-block btn-info text-white mt-4" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
